# Generated By: Cursor (Claude Sonnet 4)
name: Lint

on:
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Ansible Lint
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Install collections locally
      run: ansible-galaxy collection install -r ansible/requirements.yml -p collections/
      
    - name: Run ansible-lint
      uses: ansible/ansible-lint@main
      with:
        working_directory: ansible