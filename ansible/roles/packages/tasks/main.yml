---
# Generated By: Cursor (Claude Sonnet 4)
# Package installation tasks

- name: Update package cache
  ansible.builtin.dnf:
    update_cache: true
  become: true

- name: Install essential packages via dnf
  ansible.builtin.dnf:
    name: "{{ essential_packages }}"
    state: present
  become: true
  register: packages_package_install_result
