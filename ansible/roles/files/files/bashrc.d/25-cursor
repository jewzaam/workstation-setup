# Generated By: Cursor (Claude Sonnet 4)
# .bashrc.d/25-cursor - Cursor IDE aliases and PATH configuration
# This file is managed by ansible - user customizations go in ~/.bashrc.d/99-user

# Add Cursor CLI to PATH if installed
if [ -d "$HOME/.local/bin" ] && [ -x "$HOME/.local/bin/cursor" ]; then
    export PATH="$HOME/.local/bin:$PATH"
fi

alias code='cursor'