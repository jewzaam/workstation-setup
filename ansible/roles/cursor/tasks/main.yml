---
# Generated By: Cursor (Claude Sonnet 4.5)
# Cursor installation orchestrator

# Install Cursor AppImage (UI) with configuration
- name: Install Cursor AppImage
  ansible.builtin.include_tasks: appimage.yml

# Install Cursor CLI (cursor-agent) - independent of AppImage
- name: Install Cursor CLI
  ansible.builtin.include_tasks: cli.yml

# Install MCP Servers for Cursor
- name: Install MCP Servers
  ansible.builtin.include_tasks: mcp.yml
