---
# Generated By: Cursor (Claude Sonnet 4)
# Samba role handlers

- name: Restart smbd
  ansible.builtin.systemd:
    name: smb
    state: restarted
  become: true

- name: Reload Firewalld
  ansible.builtin.systemd:
    name: firewalld
    state: reloaded
  become: true
